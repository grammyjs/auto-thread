# Forum Utility

todo docs

## Quickstart

Install from GitHub:

```sh
npm install github:grammyjs/auto-thread
```

Use it:

```ts
import { autoThread } from "@grammyjs/auto-thread";

bot.use(autoThread());
```
